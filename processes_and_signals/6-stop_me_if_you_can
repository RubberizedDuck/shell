#!/usr/bin/env bash
# This script stops any script from running
pkill -f 4-to_infinity_and_beyond
status="$?"
echo "$status"
if [[ $status == 0 ]]
then
    echo "One or more processes matched the criteria" 1>&2
fi
if [[ $status == 1 ]]
then
    echo "No processes matched" 1>&2
fi
