#!/usr/bin/env bash
# This script stops any script from running
pkill -f 4-to_infinity_and_beyond
status="$?"
if [ $status == 0 ]
then
    pkill -f 4-to_infinity_and_beyond >&1
fi
if [ $status == 1 ]
then
    pkill -f 4-to_infinity_and_beyond >&2
fi
