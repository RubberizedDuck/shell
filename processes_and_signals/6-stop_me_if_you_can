#!/usr/bin/env bash
# This script stops any script from running
pkill -f 4-to_infinity_and_beyond
status="$?"
pass="0 One or more processes matched the criteria"
fail="1 No processes matched"
if [ $status == 0 ]
then
    echo $pass
fi
if [ $status == 1 ]
then
    echo $fail
fi
